<!DOCTYPE html>
<html>
  <head>
    <title>CarreloSquad</title>
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", defer: true %>
  </head>

  <body>
    <%= render "shared/navbar" %>
    <%= render "shared/flashes" %>
    <%= yield %>
    <%= render "shared/footer" unless (action_name == "show" || controller_name == "sessions" || (controller_name == "actors" && (action_name == "new" || action_name == "edit" || action_name == "update" || action_name == "create")) || controller_name == "links" || controller_name == "messages" || controller_name == "articles") %>
  </body>
</html>
